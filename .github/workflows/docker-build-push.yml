name: 构建 Docker 镜像

# 仅通过手动触发，并接受一个自定义镜像名作为输入
on:
  workflow_dispatch:
    inputs:
      custom_image_name:
        description: '自定义的镜像名称（例如：myapp/firefox-novnc）'
        required: true
        default: 'testff' # 默认使用当前仓库名

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1. 获取代码
      - name: 检出代码
        uses: actions/checkout@v4

      # 2. 设置 Docker 构建环境
      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. 登录到 GitHub Container Registry
      - name: 登录到 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 4. 构建并推送镜像
      - name: 构建并推送
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 使用输入的自定义镜像名，并固定标签为 latest
          tags: |
            ghcr.io/${{ github.event.inputs.custom_image_name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 5. 输出成功信息
      - name: 输出镜像信息
        run: |
          echo "✅ 镜像构建并推送成功！"
          echo "📦 镜像地址：ghcr.io/${{ github.event.inputs.custom_image_name }}:latest"
          echo "📥 拉取命令：docker pull ghcr.io/${{ github.event.inputs.custom_image_name }}:latest"
